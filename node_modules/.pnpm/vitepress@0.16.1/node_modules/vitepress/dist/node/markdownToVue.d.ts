import { MarkdownOptions } from './markdown/markdown';
import { PageData } from './shared';
export interface MarkdownCompileResult {
    vueSrc: string;
    pageData: PageData;
    deadLinks: string[];
}
export declare function createMarkdownToVueRenderFn(srcDir: string, options: MarkdownOptions | undefined, pages: string[], userDefines: Record<string, any> | undefined, isBuild?: boolean): (src: string, file: string, publicDir: string) => MarkdownCompileResult;
