import { Plugin } from 'vite';
import { SiteConfig } from './config';
export declare function createVitePressPlugin(root: string, { srcDir, configPath, alias, markdown, site, vue: userVuePluginOptions, vite: userViteConfig, pages }: SiteConfig, ssr?: boolean, pageToHashMap?: Record<string, string>): Plugin[];
